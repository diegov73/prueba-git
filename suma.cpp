#include <iostream>
using namespace std;

main() {
    int a, b, c;
    cout << "Enter two numbers: ";
    cin >> a >> b;
    c = a + b;
    cout << "The sum of the two numbers is: " << c << endl;
    return 0;
}